📖 GitHub上传完整教程
========================================

🎯 目标：将基金估值系统上传到GitHub并启用GitHub Pages

========================================

方法1：使用一键脚本（最简单）⭐推荐⭐
========================================

双击运行：🚀一键上传GitHub.bat

按照提示操作：
1. 输入GitHub用户名和邮箱（如果需要）
2. 在GitHub创建新仓库
3. 复制仓库地址并粘贴
4. 等待上传完成

⏱️ 耗时：5分钟

========================================

方法2：手动操作（详细步骤）
========================================

第一步：配置Git
--------------------
打开命令行，输入：

git config --global user.name "你的GitHub用户名"
git config --global user.email "你的GitHub邮箱"

例如：
git config --global user.name "zhangsan"
git config --global user.email "zhangsan@example.com"

第二步：初始化仓库
--------------------
cd 到项目目录，输入：

git init
git add .
git commit -m "初始提交：实时估值系统"

第三步：在GitHub创建仓库
--------------------
1. 打开浏览器，访问：https://github.com/new

2. 填写仓库信息：
   Repository name: fund-valuation-system
   Description: 基金实时估值系统 - 双重估值
   Public（公开）
   
3. 不要勾选任何初始化选项：
   ❌ Add a README file
   ❌ Add .gitignore
   ❌ Choose a license

4. 点击 Create repository

5. 复制仓库地址（HTTPS）：
   https://github.com/你的用户名/fund-valuation-system.git

第四步：连接并推送
--------------------
在命令行输入：

git remote add origin https://github.com/你的用户名/fund-valuation-system.git
git branch -M main
git push -u origin main

⚠️ 如果提示需要身份验证，请看下面的"身份验证"部分

第五步：启用GitHub Pages
--------------------
1. 打开你的GitHub仓库页面
2. 点击顶部的 Settings（设置）
3. 左侧菜单找到 Pages
4. 在 Source 下：
   - Branch: 选择 main
   - Folder: 选择 / (root)
5. 点击 Save
6. 等待5-10分钟，页面会显示网站地址

第六步：等待自动运行
--------------------
1. 回到仓库首页
2. 点击顶部的 Actions 标签
3. 看到 workflow 自动运行
4. 等待运行完成（绿色✓）
5. 数据文件会自动生成

第七步：访问网站
--------------------
网址：https://你的用户名.github.io/仓库名

例如：https://zhangsan.github.io/fund-valuation-system

🎉 完成！

========================================

身份验证方法
========================================

方法A：使用GitHub Desktop（最简单）⭐推荐⭐
--------------------
1. 下载GitHub Desktop：https://desktop.github.com/
2. 安装并登录GitHub账号
3. File → Add Local Repository
4. 选择你的项目文件夹
5. 点击 Publish repository
6. 选择 Public，点击 Publish

方法B：使用Personal Access Token
--------------------
1. 打开GitHub → Settings → Developer settings
2. Personal access tokens → Tokens (classic)
3. Generate new token (classic)
4. 勾选 repo 权限
5. 生成并复制token
6. 推送时，用户名输入GitHub用户名，密码输入token

方法C：使用SSH密钥
--------------------
1. 生成SSH密钥：
   ssh-keygen -t ed25519 -C "你的邮箱"

2. 复制公钥：
   cat ~/.ssh/id_ed25519.pub

3. 添加到GitHub：
   Settings → SSH and GPG keys → New SSH key

4. 使用SSH地址：
   git remote set-url origin git@github.com:你的用户名/仓库名.git

========================================

常见问题
========================================

问题1：git push 失败，提示需要身份验证
--------------------
解决方案：
- 使用GitHub Desktop（最简单）
- 或配置Personal Access Token
- 或使用SSH密钥

问题2：网站显示404
--------------------
原因：GitHub Pages未启用或未部署完成
解决：
1. 检查Settings → Pages是否已启用
2. 等待5-10分钟
3. 确认仓库是Public（公开）

问题3：网站显示"暂无数据"
--------------------
原因：GitHub Actions未运行或运行失败
解决：
1. 打开Actions标签
2. 手动点击 Run workflow
3. 等待运行完成
4. 刷新网站

问题4：Actions运行失败
--------------------
原因：可能是依赖安装失败
解决：
1. 查看Actions运行日志
2. 检查requirements.txt
3. 重新运行workflow

问题5：推送时提示"仓库已存在"
--------------------
解决：
git remote set-url origin https://github.com/你的用户名/新仓库名.git
git push -u origin main

========================================

GitHub Pages设置详解
========================================

位置：
仓库 → Settings → Pages

配置：
┌─────────────────────────────────┐
│ Source                          │
│ ┌─────────────────────────────┐ │
│ │ Branch: main            ▼   │ │
│ │ Folder: / (root)        ▼   │ │
│ │ [Save]                      │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘

说明：
- Branch: 选择 main（主分支）
- Folder: 选择 / (root)（根目录）
- 不要选择 /docs 或其他文件夹

保存后：
- 等待5-10分钟
- 页面会显示：
  "Your site is published at https://..."

========================================

验证部署成功
========================================

1. 检查GitHub Pages
   Settings → Pages
   应该显示：
   "Your site is published at https://..."

2. 检查Actions
   Actions标签
   应该显示：
   ✓ Auto Fund Valuation & Deploy

3. 检查文件
   仓库中应该有：
   data/funds.json

4. 访问网站
   https://你的用户名.github.io/仓库名
   应该显示基金列表

========================================

国内访问优化
========================================

如果GitHub访问慢，可以使用Gitee：

1. 注册Gitee账号：https://gitee.com/
2. 导入GitHub仓库：
   - 点击右上角 + → 从GitHub导入仓库
   - 输入GitHub仓库地址
   - 点击导入
3. 启用Gitee Pages：
   - 仓库 → 服务 → Gitee Pages
   - 点击启动
4. 访问：https://你的用户名.gitee.io/仓库名

详见：Gitee-Pages教程（国内最快）.txt

========================================

自动更新说明
========================================

GitHub Actions会自动运行：
- 时间：工作日9:00-15:00
- 频率：每30分钟
- 任务：抓取最新数据

手动触发：
1. 打开Actions标签
2. 选择 Auto Fund Valuation & Deploy
3. 点击 Run workflow
4. 等待运行完成

========================================

完整流程总结
========================================

1. 配置Git（用户名、邮箱）
2. 初始化本地仓库（git init）
3. 添加文件（git add .）
4. 提交（git commit）
5. 在GitHub创建仓库
6. 连接远程仓库（git remote add）
7. 推送（git push）
8. 启用GitHub Pages
9. 等待Actions运行
10. 访问网站

⏱️ 总耗时：10-15分钟

========================================

🚀 快速开始
========================================

最简单的方法：

双击运行：🚀一键上传GitHub.bat

按照提示操作即可！

========================================

需要帮助？
========================================

查看详细文档：
- GitHub-Pages设置步骤（图文）.txt
- 国内访问解决方案.txt
- ⚡立即体验实时估值.txt

或者使用GitHub Desktop（最简单）：
https://desktop.github.com/

========================================

祝部署顺利！📈💰
