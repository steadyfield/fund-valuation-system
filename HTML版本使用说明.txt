╔═══════════════════════════════════════════════════════════════════╗
║              📄 HTML 版本使用说明                                 ║
╚═══════════════════════════════════════════════════════════════════╝

【优势】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 无需 npm install
✅ 无需 React 构建
✅ 直接打开就能用
✅ 文件更小，加载更快
✅ 更容易理解和修改


【部署到 Vercel】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

方法 1：通过 Git（推荐）

1. 提交代码：
   git add index.html
   git commit -m "Switch to HTML version"
   git push

2. Vercel 会自动部署
3. 等待 1-2 分钟
4. 访问你的网站


方法 2：直接在 Vercel 上传

1. 登录 vercel.com
2. 进入你的项目
3. Settings → General → Root Directory
4. 设置为 "." (当前目录)
5. 重新部署


【部署到 GitHub Pages】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 提交代码：
   git add index.html
   git commit -m "Switch to HTML version"
   git push

2. 进入 GitHub 仓库 Settings → Pages
3. Source 选择 "main" 分支
4. Root 选择 "/ (root)"
5. 保存

6. 访问：https://你的用户名.github.io/fund-valuation-system


【本地测试】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

方法 1：使用 Python（推荐）
   python -m http.server 8000
   然后访问：http://localhost:8000

方法 2：使用 Node.js
   npx http-server
   然后访问：http://localhost:8080

方法 3：使用 VS Code
   安装 "Live Server" 插件
   右键 index.html → "Open with Live Server"


【文件结构】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

项目根目录/
├── index.html              ← 主页面（新）
├── data/
│   └── funds.json         ← 数据文件
├── scripts/
│   └── fetch_data.py      ← 数据抓取脚本
└── .github/workflows/
    └── update_data.yml    ← 自动化配置


【修改数据路径】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

如果数据文件在不同位置，修改 index.html 中的这一行：

const response = await fetch('/data/funds.json');

改为你的实际路径，例如：
- 同级目录：'./funds.json'
- 子目录：'./public/data/funds.json'
- 绝对路径：'/data/funds.json'


【自定义样式】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

所有样式都在 <style> 标签中，可以直接修改：

1. 修改颜色：
   background-color: #0d1117;  ← 背景色
   color: #c9d1d9;             ← 文字颜色

2. 修改字体：
   font-family: 'Your Font', sans-serif;

3. 修改布局：
   grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));


【添加新功能】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

所有 JavaScript 代码都在 <script> 标签中：

1. loadData() - 加载数据
2. renderFunds() - 渲染基金列表
3. showError() - 显示错误信息

可以直接在这些函数中添加你的逻辑。


【与 React 版本对比】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

HTML 版本：
  ✅ 简单直接
  ✅ 无需构建
  ✅ 文件更小
  ✅ 加载更快
  ❌ 代码组织性较差（大型项目）

React 版本：
  ✅ 代码组织好
  ✅ 易于扩展
  ✅ 组件化开发
  ❌ 需要构建
  ❌ 文件较大


【推荐使用场景】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

使用 HTML 版本：
  - 简单的展示页面
  - 不需要复杂交互
  - 想要快速部署
  - 不熟悉 React

使用 React 版本：
  - 需要复杂交互
  - 计划添加更多功能
  - 熟悉 React 开发
  - 需要组件化开发


【立即部署】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 提交代码：
   git add index.html
   git commit -m "Add HTML version"
   git push

2. 等待部署完成（1-2 分钟）

3. 访问你的网站

4. 如果显示"暂无数据"，运行：
   python scripts/fetch_data.py
   git add data/funds.json
   git commit -m "Add data"
   git push


完成！现在你有一个纯 HTML 的基金估值网站了！
